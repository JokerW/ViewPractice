<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    package="com.example.joker.viewpractice" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.SimpleCoordinatorActivity" />
        <activity android:name=".activity.SwipeBehaviorExampleActivity" />
        <activity android:name=".activity.MaterialUpConceptActivity" />
        <activity android:name=".activity.FlexibleSpaceExampleActivity" />
        <activity android:name=".activity.IOActivityExample" />


    </application>

</manifest>
